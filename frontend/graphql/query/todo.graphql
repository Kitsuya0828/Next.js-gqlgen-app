query getTodo {
    todos {
        id
        text
    }
}

mutation createTodo($text: String!) {
    createTodo(input: { text: $text, userId: "1" }) {
        user {
            id
        }
        text
        done
    }
}
