query getTodo {
    todos {
        id
        content
    }
}

mutation createTodo($content: String!) {
    createTodo(input: { content: $content, userId: "1" }) {
        user {
            id
        }
        content
        done
    }
}
